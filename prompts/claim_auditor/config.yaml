# Frontier tier: tier classification is the control that buys v2 its
# boldness (E2) — a mis-tiered bindable claim escapes verification
# entirely (the tier-camouflage failure the poison set measures).
# tools: none — the catalog is inlined by code; the auditor never
# searches (the fact sheet is search-invisible by design, B34(26)).
tier: frontier
tools: []
