# S5 least privilege: the architect consumes an inlined brief digest,
# the inlined reference outline, and optional lead feedback — it drives
# no retrieval and touches no tools. Frontier tier: outline adaptation
# is the one Path-B cognitive job (ARCHITECTURE roster).
tier: frontier
tools: []
