{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rfp-engine.invalid/rfp-engine-v2/manifest.schema.json",
  "title": "ServiceLineManifest",
  "description": "Core-content manifest for one service line: the content every credible response must cover. P6 renders each obligation onto the pursuit plan's obligations[] checklist (B16's consumer); the manifest is digest-visible run config. Duplicate obligation ids are rejected by the loader (jsonschema cannot express the check).",
  "type": "object",
  "required": ["service_line", "obligations"],
  "additionalProperties": false,
  "properties": {
    "service_line": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$" },
    "obligations": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "title"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$" },
          "title": { "type": "string" },
          "description": { "type": "string" }
        }
      }
    }
  }
}
